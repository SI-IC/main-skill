---
id: 5
title: Триггеры N/J: нетривиальность как дельта new_string vs old_string, не весь new_string
priority: medium
status: open
created: 2026-07-16T09:12:51Z
updated: 2026-07-16T09:12:51Z
---
countNonTrivialDiffLines (hooks/lib/checks.js:~1596) суммирует ВСЕ непустые не-comment строки каждого Edit.new_string. Механический rename/extract с широким контекстным блоком (old_string/new_string расширены ради уникальности якоря) надувает счётчик до порога ≥20 → форсит премортем (триггер N) и self-review (триггер J) на чисто рефакторинговой сессии без единого внешнего контракта.

Что сделать: считать нетривиальные строки как line-diff new_string против old_string (например, множество строк new минус строки old; оценить и вариант LCS — перестановки строк не должны считаться добавлением).

Осторожно: метрика ОБЩАЯ для J и N — изменение сдвинет порог срабатывания обоих триггеров у живых юзеров плагина; нужны регресс-тесты на существующие пороговые кейсы (verify-changes.test.js: BIG_DIFF-фикстуры, 'тривиальная правка', 'правка docs не учитывается').

Источник: находка code-review (#5) и premortem-review (#5) от 2026-07-16 при имплементации триггера N (v1.10.0, коммит cac6e2d); отражено в CLAUDE.md-памяти premortem-edge-case-plan → «Отложено».
